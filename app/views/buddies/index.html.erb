<div class="page-container">

<div class="second-container">

 <div class="banner-container">
    <div class="banner" style="background-image: linear-gradient(rgba(0,0,0,0.4),rgba(0,0,0,0.4)), url(<%= image_path "Cover.jpg" %>);">
      <div class="container">
        <h1><strong>Boost ton projet web avec l'elite des buddies !</strong></h1>
      </div>
  </div>

  <!-- Barre de filtrage -->
<div class="filter-bar">
  <!-- Champ de filtrage pour le prix -->
  <%# <label for="price-filter">Price:</label>
  <input type="text" id="price-filter" name="price-filter" placeholder="Enter price"> %>

  <!-- Champ de filtrage pour la date de début -->
  <label for="start-date-filter">Start Date:</label>
  <input type="date" id="start-date-filter" name="start-date-filter">

  <!-- Champ de filtrage pour la date de fin -->
  <label for="end-date-filter">End Date:</label>
  <input type="date" id="end-date-filter" name="end-date-filter">

  <!-- Bouton de soumission du formulaire de filtrage -->
  <button type="button" onclick="filterBuddies()">Filter</button>
</div>


 <div class="container my-5" id="buddies-container">
        <div class="row">
            <% @buddies.each do |buddy| %>
                <div class="col-md-4 mb-4">
                    <div class="card h-100">
                        <a href="<%= buddy_path(buddy) %>" class="text-decoration-none" data-name="<%= buddy.name %>" data-options="<%= buddy.options %>" data-price-per-day="<%= buddy.price_per_day %>">
                            <div class="d-flex justify-content-center mt-3">
                                <% if buddy.photo.attached? %>
                                    <%= cl_image_tag buddy.photo.key, class: "rounded-circle img-thumbnail", width: "100", height: "100" %>
                                <% else %>
                                    <img src="images/deliveroo.png" alt="Buddy Photo" class="rounded-circle img-thumbnail" width="100" height="100">
                                <% end %>
                            </div>
                            <div class="card-body text-center">
                                <h5 class="card-title"><%= buddy.name %></h5>
                                <p class="card-text"><%= buddy.description %></p>
                                <h2 class="card-trip-pricing"><%= buddy.price_per_day %> Bitcoin par jour</h2>
                                <button class="btn btn-primary mt-2">Voir le buddy</button>
                            </div>
                        </a>
                    </div>
                </div>
            <% end %>
        </div>
    </div>

<script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>


<%# <script>
  function filterBuddies() {
    // Récupérer les valeurs des champs de filtrage
    var priceFilter = document.getElementById('price-filter').value;
    var startDateFilter = document.getElementById('start-date-filter').value;
    var endDateFilter = document.getElementById('end-date-filter').value;

    // Effectuer le filtrage des buddies selon les critères choisis
    // Vous pouvez utiliser ces valeurs pour effectuer des requêtes AJAX ou filtrer les buddies côté client
    // Par exemple, vous pouvez utiliser les valeurs pour afficher uniquement les buddies qui correspondent aux critères sélectionnés
  }
</script> %>
</div>
</div>
